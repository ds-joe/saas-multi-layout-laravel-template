// Layout
import DashboardLayout from '@/Layouts/Dashboard';

const Home: RP = () => {
  return (
    <div className="m-4">

    </div>
  );
};

Home.layout = (page) => <DashboardLayout children={page} />;

export default Home;
